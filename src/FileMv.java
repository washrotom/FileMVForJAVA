import java.io.File;

//Files.move(soure, target, option);
//이식을 목적으로 만들기 때문에 메인에서는 인수를 받기만 한다.
public class FileMv {

	public static void main(String[] args) {

	}
	//파일의 최근수정날짜를 읽는다
	//일치하는 폴더가 없다 -> 그 날짜로 만든다 -> 스트링 뒤에 그 날짜의 데이터를 넣는다.
	public void Move(String targetDir) {
		String TargetDir = targetDir;
		
	}

}

